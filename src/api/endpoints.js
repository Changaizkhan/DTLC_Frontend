/**
 * Re-exports server routes as `endpoints` for backward compatibility.
 * The single source of truth is src/routes/serverRoutes.js
 */
export { serverRoutes as endpoints } from '../routes/serverRoutes';
